#include <gtest/gtest.h>
#include "/home/parker/desktop/учеба/4_курс/Субботин/CalculatorProject/calculator.h"

TEST(UI_Test, DisplayAddition) {
    // Здесь можно имитировать проверку вывода.
    // Например, проверить вывод в консоль (в реальности нужно использовать фреймворк для UI тестирования).
    EXPECT_TRUE(true); // Заглушка для примера
}

TEST(UI_Test, DisplayErrorOnDivisionByZero) {
    // Проверка обработки ошибки.
    EXPECT_TRUE(true); // Заглушка для примера
}
